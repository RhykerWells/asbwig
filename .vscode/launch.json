{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "summit Local",
            "type":"go",
            "request": "launch",
            "mode": "debug",
            "envFile": "${workspaceFolder}/app.env",
            "program": "${workspaceFolder}/cmd/summit/main.go",
            "output": "${workspaceFolder}/cmd/summit/summit",
        }
    ]
}