// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ModerationMute is an object representing the database table.
type ModerationMute struct {
	GuildID  string            `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	UserID   string            `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Roles    types.StringArray `boil:"roles" json:"roles" toml:"roles" yaml:"roles"`
	UnmuteAt time.Time         `boil:"unmute_at" json:"unmute_at" toml:"unmute_at" yaml:"unmute_at"`

	R *moderationMuteR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L moderationMuteL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ModerationMuteColumns = struct {
	GuildID  string
	UserID   string
	Roles    string
	UnmuteAt string
}{
	GuildID:  "guild_id",
	UserID:   "user_id",
	Roles:    "roles",
	UnmuteAt: "unmute_at",
}

var ModerationMuteTableColumns = struct {
	GuildID  string
	UserID   string
	Roles    string
	UnmuteAt string
}{
	GuildID:  "moderation_mutes.guild_id",
	UserID:   "moderation_mutes.user_id",
	Roles:    "moderation_mutes.roles",
	UnmuteAt: "moderation_mutes.unmute_at",
}

// Generated where

var ModerationMuteWhere = struct {
	GuildID  whereHelperstring
	UserID   whereHelperstring
	Roles    whereHelpertypes_StringArray
	UnmuteAt whereHelpertime_Time
}{
	GuildID:  whereHelperstring{field: "\"moderation_mutes\".\"guild_id\""},
	UserID:   whereHelperstring{field: "\"moderation_mutes\".\"user_id\""},
	Roles:    whereHelpertypes_StringArray{field: "\"moderation_mutes\".\"roles\""},
	UnmuteAt: whereHelpertime_Time{field: "\"moderation_mutes\".\"unmute_at\""},
}

// ModerationMuteRels is where relationship names are stored.
var ModerationMuteRels = struct {
	Guild string
}{
	Guild: "Guild",
}

// moderationMuteR is where relationships are stored.
type moderationMuteR struct {
	Guild *ModerationConfig `boil:"Guild" json:"Guild" toml:"Guild" yaml:"Guild"`
}

// NewStruct creates a new relationship struct
func (*moderationMuteR) NewStruct() *moderationMuteR {
	return &moderationMuteR{}
}

func (o *ModerationMute) GetGuild() *ModerationConfig {
	if o == nil {
		return nil
	}

	return o.R.GetGuild()
}

func (r *moderationMuteR) GetGuild() *ModerationConfig {
	if r == nil {
		return nil
	}

	return r.Guild
}

// moderationMuteL is where Load methods for each relationship are stored.
type moderationMuteL struct{}

var (
	moderationMuteAllColumns            = []string{"guild_id", "user_id", "roles", "unmute_at"}
	moderationMuteColumnsWithoutDefault = []string{"guild_id", "user_id", "roles", "unmute_at"}
	moderationMuteColumnsWithDefault    = []string{}
	moderationMutePrimaryKeyColumns     = []string{"guild_id", "user_id"}
	moderationMuteGeneratedColumns      = []string{}
)

type (
	// ModerationMuteSlice is an alias for a slice of pointers to ModerationMute.
	// This should almost always be used instead of []ModerationMute.
	ModerationMuteSlice []*ModerationMute

	moderationMuteQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	moderationMuteType                 = reflect.TypeOf(&ModerationMute{})
	moderationMuteMapping              = queries.MakeStructMapping(moderationMuteType)
	moderationMutePrimaryKeyMapping, _ = queries.BindMapping(moderationMuteType, moderationMuteMapping, moderationMutePrimaryKeyColumns)
	moderationMuteInsertCacheMut       sync.RWMutex
	moderationMuteInsertCache          = make(map[string]insertCache)
	moderationMuteUpdateCacheMut       sync.RWMutex
	moderationMuteUpdateCache          = make(map[string]updateCache)
	moderationMuteUpsertCacheMut       sync.RWMutex
	moderationMuteUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single moderationMute record from the query using the global executor.
func (q moderationMuteQuery) OneG(ctx context.Context) (*ModerationMute, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single moderationMute record from the query.
func (q moderationMuteQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ModerationMute, error) {
	o := &ModerationMute{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for moderation_mutes")
	}

	return o, nil
}

// AllG returns all ModerationMute records from the query using the global executor.
func (q moderationMuteQuery) AllG(ctx context.Context) (ModerationMuteSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all ModerationMute records from the query.
func (q moderationMuteQuery) All(ctx context.Context, exec boil.ContextExecutor) (ModerationMuteSlice, error) {
	var o []*ModerationMute

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ModerationMute slice")
	}

	return o, nil
}

// CountG returns the count of all ModerationMute records in the query using the global executor
func (q moderationMuteQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all ModerationMute records in the query.
func (q moderationMuteQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count moderation_mutes rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q moderationMuteQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q moderationMuteQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if moderation_mutes exists")
	}

	return count > 0, nil
}

// Guild pointed to by the foreign key.
func (o *ModerationMute) Guild(mods ...qm.QueryMod) moderationConfigQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"guild_id\" = ?", o.GuildID),
	}

	queryMods = append(queryMods, mods...)

	return ModerationConfigs(queryMods...)
}

// LoadGuild allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (moderationMuteL) LoadGuild(ctx context.Context, e boil.ContextExecutor, singular bool, maybeModerationMute interface{}, mods queries.Applicator) error {
	var slice []*ModerationMute
	var object *ModerationMute

	if singular {
		var ok bool
		object, ok = maybeModerationMute.(*ModerationMute)
		if !ok {
			object = new(ModerationMute)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeModerationMute)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeModerationMute))
			}
		}
	} else {
		s, ok := maybeModerationMute.(*[]*ModerationMute)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeModerationMute)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeModerationMute))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &moderationMuteR{}
		}
		args[object.GuildID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &moderationMuteR{}
			}

			args[obj.GuildID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`moderation_config`),
		qm.WhereIn(`moderation_config.guild_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ModerationConfig")
	}

	var resultSlice []*ModerationConfig
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ModerationConfig")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for moderation_config")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for moderation_config")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Guild = foreign
		if foreign.R == nil {
			foreign.R = &moderationConfigR{}
		}
		foreign.R.GuildModerationMutes = append(foreign.R.GuildModerationMutes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GuildID == foreign.GuildID {
				local.R.Guild = foreign
				if foreign.R == nil {
					foreign.R = &moderationConfigR{}
				}
				foreign.R.GuildModerationMutes = append(foreign.R.GuildModerationMutes, local)
				break
			}
		}
	}

	return nil
}

// SetGuildG of the moderationMute to the related item.
// Sets o.R.Guild to related.
// Adds o to related.R.GuildModerationMutes.
// Uses the global database handle.
func (o *ModerationMute) SetGuildG(ctx context.Context, insert bool, related *ModerationConfig) error {
	return o.SetGuild(ctx, boil.GetContextDB(), insert, related)
}

// SetGuild of the moderationMute to the related item.
// Sets o.R.Guild to related.
// Adds o to related.R.GuildModerationMutes.
func (o *ModerationMute) SetGuild(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ModerationConfig) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"moderation_mutes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"guild_id"}),
		strmangle.WhereClause("\"", "\"", 2, moderationMutePrimaryKeyColumns),
	)
	values := []interface{}{related.GuildID, o.GuildID, o.UserID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GuildID = related.GuildID
	if o.R == nil {
		o.R = &moderationMuteR{
			Guild: related,
		}
	} else {
		o.R.Guild = related
	}

	if related.R == nil {
		related.R = &moderationConfigR{
			GuildModerationMutes: ModerationMuteSlice{o},
		}
	} else {
		related.R.GuildModerationMutes = append(related.R.GuildModerationMutes, o)
	}

	return nil
}

// ModerationMutes retrieves all the records using an executor.
func ModerationMutes(mods ...qm.QueryMod) moderationMuteQuery {
	mods = append(mods, qm.From("\"moderation_mutes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"moderation_mutes\".*"})
	}

	return moderationMuteQuery{q}
}

// FindModerationMuteG retrieves a single record by ID.
func FindModerationMuteG(ctx context.Context, guildID string, userID string, selectCols ...string) (*ModerationMute, error) {
	return FindModerationMute(ctx, boil.GetContextDB(), guildID, userID, selectCols...)
}

// FindModerationMute retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindModerationMute(ctx context.Context, exec boil.ContextExecutor, guildID string, userID string, selectCols ...string) (*ModerationMute, error) {
	moderationMuteObj := &ModerationMute{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"moderation_mutes\" where \"guild_id\"=$1 AND \"user_id\"=$2", sel,
	)

	q := queries.Raw(query, guildID, userID)

	err := q.Bind(ctx, exec, moderationMuteObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from moderation_mutes")
	}

	return moderationMuteObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ModerationMute) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ModerationMute) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no moderation_mutes provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(moderationMuteColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	moderationMuteInsertCacheMut.RLock()
	cache, cached := moderationMuteInsertCache[key]
	moderationMuteInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			moderationMuteAllColumns,
			moderationMuteColumnsWithDefault,
			moderationMuteColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(moderationMuteType, moderationMuteMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(moderationMuteType, moderationMuteMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"moderation_mutes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"moderation_mutes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into moderation_mutes")
	}

	if !cached {
		moderationMuteInsertCacheMut.Lock()
		moderationMuteInsertCache[key] = cache
		moderationMuteInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single ModerationMute record using the global executor.
// See Update for more documentation.
func (o *ModerationMute) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the ModerationMute.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ModerationMute) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	moderationMuteUpdateCacheMut.RLock()
	cache, cached := moderationMuteUpdateCache[key]
	moderationMuteUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			moderationMuteAllColumns,
			moderationMutePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update moderation_mutes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"moderation_mutes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, moderationMutePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(moderationMuteType, moderationMuteMapping, append(wl, moderationMutePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update moderation_mutes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for moderation_mutes")
	}

	if !cached {
		moderationMuteUpdateCacheMut.Lock()
		moderationMuteUpdateCache[key] = cache
		moderationMuteUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q moderationMuteQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q moderationMuteQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for moderation_mutes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for moderation_mutes")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ModerationMuteSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ModerationMuteSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moderationMutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"moderation_mutes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, moderationMutePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in moderationMute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all moderationMute")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ModerationMute) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ModerationMute) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no moderation_mutes provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(moderationMuteColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	moderationMuteUpsertCacheMut.RLock()
	cache, cached := moderationMuteUpsertCache[key]
	moderationMuteUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			moderationMuteAllColumns,
			moderationMuteColumnsWithDefault,
			moderationMuteColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			moderationMuteAllColumns,
			moderationMutePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert moderation_mutes, could not build update column list")
		}

		ret := strmangle.SetComplement(moderationMuteAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(moderationMutePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert moderation_mutes, could not build conflict column list")
			}

			conflict = make([]string, len(moderationMutePrimaryKeyColumns))
			copy(conflict, moderationMutePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"moderation_mutes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(moderationMuteType, moderationMuteMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(moderationMuteType, moderationMuteMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert moderation_mutes")
	}

	if !cached {
		moderationMuteUpsertCacheMut.Lock()
		moderationMuteUpsertCache[key] = cache
		moderationMuteUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single ModerationMute record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ModerationMute) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single ModerationMute record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ModerationMute) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ModerationMute provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), moderationMutePrimaryKeyMapping)
	sql := "DELETE FROM \"moderation_mutes\" WHERE \"guild_id\"=$1 AND \"user_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from moderation_mutes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for moderation_mutes")
	}

	return rowsAff, nil
}

func (q moderationMuteQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q moderationMuteQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no moderationMuteQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from moderation_mutes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for moderation_mutes")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o ModerationMuteSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ModerationMuteSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moderationMutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"moderation_mutes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, moderationMutePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from moderationMute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for moderation_mutes")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ModerationMute) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no ModerationMute provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ModerationMute) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindModerationMute(ctx, exec, o.GuildID, o.UserID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ModerationMuteSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty ModerationMuteSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ModerationMuteSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ModerationMuteSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moderationMutePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"moderation_mutes\".* FROM \"moderation_mutes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, moderationMutePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ModerationMuteSlice")
	}

	*o = slice

	return nil
}

// ModerationMuteExistsG checks if the ModerationMute row exists.
func ModerationMuteExistsG(ctx context.Context, guildID string, userID string) (bool, error) {
	return ModerationMuteExists(ctx, boil.GetContextDB(), guildID, userID)
}

// ModerationMuteExists checks if the ModerationMute row exists.
func ModerationMuteExists(ctx context.Context, exec boil.ContextExecutor, guildID string, userID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"moderation_mutes\" where \"guild_id\"=$1 AND \"user_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, guildID, userID)
	}
	row := exec.QueryRowContext(ctx, sql, guildID, userID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if moderation_mutes exists")
	}

	return exists, nil
}

// Exists checks if the ModerationMute row exists.
func (o *ModerationMute) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ModerationMuteExists(ctx, exec, o.GuildID, o.UserID)
}
